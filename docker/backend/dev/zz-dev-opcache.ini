; Bind-mounted into /usr/local/etc/php/conf.d/ by docker-compose.dev.yml.
; Disables OPcache so PHP edits under ./backend are reflected on the very next request,
; not after opcache.revalidate_freq. Production keeps the image's default OPcache config.
opcache.enable=0
opcache.enable_cli=0
