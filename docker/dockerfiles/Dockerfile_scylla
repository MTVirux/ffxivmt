# Config, entrypoint and schema all arrive via the compose bind mounts, so a
# `git pull` on the Scylla VM takes effect without an image rebuild.
FROM scylladb/scylla:5.1

RUN mkdir -p /backup

ENTRYPOINT ["/usr/local/bin/run_entrypoints.sh"]
