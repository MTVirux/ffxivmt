<h1>USAGE:</h1>

scope:
    - Param index: 0
    - Type: string
    - Valid values: key, item, world, all
      Description:  The scope of the search. If scope is 'all', then the other two params are ignored. 
                    If the scope is key, both params
      Example: "key"